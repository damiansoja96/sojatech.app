{
  "name": "portfolio-website",
  "type": "module",
  "version": "1.0.0",
  "packageManager": "npm",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
